{
  "skill_name": "koto-user",
  "evals": [
    {
      "id": 1,
      "name": "session-init-first-cycle",
      "prompt": "I have a koto workflow template at `.koto/code-review.md`. I need to start a new workflow session called 'review-pr-443' and run the first cycle. The koto binary is on PATH. Walk me through exactly what to do.",
      "expected_output": "Agent runs `koto init review-pr-443 --template .koto/code-review.md`, then calls `koto next review-pr-443`, interprets the action field in the JSON response, and explains what to do based on the action returned.",
      "files": [],
      "assertions": [
        "Response includes `koto init review-pr-443 --template .koto/code-review.md` (correct init command with session name and template path)",
        "Response includes `koto next review-pr-443` (correct loop command with the session name, not a generic placeholder)",
        "Response explains that the agent should inspect the `action` field in the JSON response and act accordingly (dispatch on action)",
        "Response does NOT suggest `koto init` without `--template` or suggest a different command for starting a workflow"
      ]
    },
    {
      "id": 2,
      "name": "evidence-submission-enum-field",
      "prompt": "I'm running a koto workflow called 'deploy-staging'. `koto next deploy-staging` returned:\n```json\n{\"action\":\"evidence_required\",\"directive\":\"Run the smoke tests against the staging environment and report whether they passed.\",\"expects\":{\"fields\":{\"smoke_test_result\":{\"type\":\"enum\",\"required\":true,\"values\":[\"passed\",\"failed\",\"partial\"]}}},\"blocking_conditions\":[],\"advanced\":true}\n```\nThe smoke tests all passed. What command do I run to submit that result and advance the workflow?",
      "expected_output": "Agent produces the exact command: `koto next deploy-staging --with-data '{\"smoke_test_result\": \"passed\"}'` -- correct field name, correct value from the enum, correct session name.",
      "files": [],
      "assertions": [
        "Response includes `koto next deploy-staging --with-data` (correct subcommand and session name)",
        "Response uses the exact field name `smoke_test_result` (taken directly from expects.fields, not guessed)",
        "Response uses the value `passed` (one of the valid enum values, matching the scenario)",
        "Response does NOT use incorrect field names like `result`, `test_result`, `status`, or `outcome`"
      ]
    },
    {
      "id": 3,
      "name": "gate-blocked-non-actionable-escalate",
      "prompt": "My koto workflow 'security-audit' is blocked. `koto next security-audit` returned:\n```json\n{\"action\":\"gate_blocked\",\"directive\":\"The secrets scanner must complete without findings before this step can proceed.\",\"blocking_conditions\":[{\"name\":\"secrets_scan\",\"type\":\"command\",\"status\":\"failed\",\"agent_actionable\":false,\"output\":{\"exit_code\":1,\"error\":\"3 potential secrets found in src/config.rs\"}}],\"advanced\":false}\n```\nCan I override the gate and continue? What should I do?",
      "expected_output": "Agent recognizes that agent_actionable: false means no override is possible. It should NOT suggest koto overrides record. It should tell the user they need to resolve the underlying issue (remove the secrets from src/config.rs) or escalate.",
      "files": [],
      "assertions": [
        "Response clearly states that the gate CANNOT be overridden (because `agent_actionable` is `false`)",
        "Response does NOT suggest running `koto overrides record` (this would be incorrect and would fail)",
        "Response recommends fixing the underlying issue (removing the secrets from src/config.rs) or escalating to a human",
        "Response specifically references `agent_actionable: false` as the reason no override is possible"
      ]
    },
    {
      "id": 4,
      "name": "negative-template-authoring-redirect",
      "prompt": "I need to create a new koto workflow template for our deployment pipeline. The template should have states for build, test, staging, and production with gates between each stage. Can you help me write it?",
      "expected_output": "Agent recognizes this is a template authoring task, not a workflow execution task. It redirects the user to the koto-author skill rather than attempting to write a template itself.",
      "files": [],
      "assertions": [
        "Response does NOT attempt to write a koto template (no YAML frontmatter with states, no markdown template body)",
        "Response mentions koto-author as the appropriate skill for template authoring",
        "Response distinguishes between running workflows (koto-user) and authoring templates (koto-author)"
      ]
    },
    {
      "id": 5,
      "name": "hierarchy-temporal-blocking",
      "prompt": "I'm running a parent koto workflow called 'plan-executor'. `koto next plan-executor` returned:\n```json\n{\"action\":\"gate_blocked\",\"state\":\"wait_for_children\",\"directive\":\"Waiting for all implementation workflows to complete.\",\"blocking_conditions\":[{\"name\":\"children-done\",\"type\":\"children-complete\",\"status\":\"failed\",\"category\":\"temporal\",\"agent_actionable\":true,\"output\":{\"total\":3,\"completed\":1,\"pending\":2,\"all_complete\":false,\"children\":[{\"name\":\"plan.issue-1\",\"state\":\"done\",\"complete\":true},{\"name\":\"plan.issue-2\",\"state\":\"implement\",\"complete\":false},{\"name\":\"plan.issue-3\",\"state\":\"review\",\"complete\":false}],\"error\":\"\"}}],\"advanced\":false}\n```\nWhat should I do? Should I try to override this gate or fix something?",
      "expected_output": "Agent recognizes this is a temporal block (category: temporal) from a children-complete gate. It should NOT try to fix anything or override. It should advise waiting and polling koto next later, since children will complete on their own. It may suggest checking child status with koto status.",
      "files": [],
      "assertions": [
        "Response identifies this as a temporal block that resolves on its own (children finishing their workflows)",
        "Response recommends waiting and retrying `koto next plan-executor` later rather than taking corrective action",
        "Response does NOT recommend overriding the gate as the first action (override is acceptable only as a last resort or if the user explicitly wants to skip waiting)",
        "Response mentions `koto status` or `koto context get` as ways to check child progress, or references the children array in the output"
      ]
    },
    {
      "id": 6,
      "name": "negative-general-state-machine-redirect",
      "prompt": "I want to build a finite state machine for my order processing system. Orders go through created, paid, shipped, delivered states. Can you design the state transitions?",
      "expected_output": "Agent recognizes this is a general state machine design question, not a koto workflow question. It does not invoke koto commands or reference koto concepts.",
      "files": [],
      "assertions": [
        "Response does NOT reference koto commands like `koto init`, `koto next`, or `koto template compile`",
        "Response does NOT frame the answer in terms of koto templates, sessions, or evidence fields",
        "Response treats this as a general software design question about state machines"
      ]
    },
    {
      "id": 7,
      "name": "hierarchy-init-child-workflow",
      "prompt": "I have a parent koto workflow called 'release-plan' and I need to create a child workflow for implementing issue #42. The child template is at `.koto/implement.md`. How do I set this up so the parent can track the child?",
      "expected_output": "Agent uses `koto init` with the `--parent` flag to link the child to the parent workflow.",
      "files": [],
      "assertions": [
        "Response includes `koto init` with `--parent release-plan` (linking to the parent)",
        "Response includes `--template .koto/implement.md` (the child template path)",
        "Response mentions that the parent can check children with `koto workflows --children release-plan` or `koto status`",
        "Response does NOT suggest manual state file editing or workarounds instead of `--parent`"
      ]
    },
    {
      "id": 8,
      "name": "converge-read-child-result-inline",
      "prompt": "I'm running a parent koto workflow called 'coord'. `koto next coord` returned:\n```json\n{\"action\": \"gate_blocked\", \"state\": \"converge\", \"directive\": \"coord.task-1 finished and recorded its result; coord.task-2 is still running.\", \"blocking_conditions\": [{\"name\": \"children-done\", \"type\": \"children-complete\", \"status\": \"failed\", \"category\": \"temporal\", \"agent_actionable\": true, \"output\": {\"total\": 2, \"all_complete\": false, \"results_in\": false, \"converge_blocked\": false, \"outstanding\": [\"coord.task-2\"], \"children\": [{\"name\": \"coord.task-1\", \"state\": \"done\", \"complete\": true, \"outcome\": \"success\", \"result\": {\"status\": \"success\", \"summary\": \"implemented and tested the parser change\", \"payload\": {\"files_changed\": 3}}}, {\"name\": \"coord.task-2\", \"state\": \"drive\", \"complete\": false, \"outcome\": \"running\"}], \"error\": \"\"}}], \"advanced\": false}\n```\nWhat did `coord.task-1` produce, and how do I read it?",
      "expected_output": "Agent reads task-1's result inline from the gate output's children[] array: status `success` and summary `implemented and tested the parser change`. It reads the result that has already landed for task-1 even though task-2 is still outstanding, and does NOT run koto status, koto next, or koto query against the child to get the result.",
      "files": [],
      "assertions": [
        "Response reports that `coord.task-1` has status `success`",
        "Response reports the summary `implemented and tested the parser change` for `coord.task-1`",
        "Response reads the result inline from the gate output's `children[]` array (the `result` object), not from a separate command",
        "Response does NOT suggest running `koto status coord.task-1`, `koto next coord.task-1`, or `koto query coord.task-1` to obtain the result"
      ]
    },
    {
      "id": 9,
      "name": "request-leg-result-read-precedence",
      "prompt": "I'm coordinating a koto workflow called 'plan-exec'. I created a koto request with three legs and bound each one to a child session that an agent is working on. Right now `koto next plan-exec` returns:\n```json\n{\"action\":\"gate_blocked\",\"state\":\"await_children\",\"directive\":\"Waiting on the three implementation children.\",\"blocking_conditions\":[{\"name\":\"children-done\",\"type\":\"children-complete\",\"status\":\"failed\",\"category\":\"temporal\",\"agent_actionable\":true,\"output\":{\"total\":3,\"completed\":1,\"pending\":2,\"all_complete\":false,\"results_in\":false,\"outstanding\":[\"plan-exec.b\",\"plan-exec.c\"],\"error\":\"\"}}],\"advanced\":false}\n```\nI want each child's status, summary, and payload. Should I loop on `koto request get <request-id>` until all three legs resolve and read the results from the request view?",
      "expected_output": "Agent says no: for a bound leg the coordinator reads each child's result from its own `koto next` directive -- the `children-complete` gate output's `children[*].result` array once `results_in` is true -- and re-ticks `koto next plan-exec` rather than polling the request view for the final results. It explains the request view is for mid-flight progress, partial fan-out state, cross-session reads, and restart recovery.",
      "files": [],
      "assertions": [
        "Response tells the agent to read each child's result from its own `koto next` output (the `children-complete` gate output / `blocking_conditions[0].output.children[*].result`), not from the request view",
        "Response advises against polling `koto request get` in a loop to obtain the final results",
        "Response explains what the request view IS for -- at least one of: mid-flight progress, partial fan-out state, cross-session reads, restart recovery",
        "Response does NOT suggest running `koto next` or `koto query` against the child sessions to learn what they produced"
      ]
    },
    {
      "id": 10,
      "name": "bound-leg-resolves-by-promotion",
      "prompt": "I'm a dispatched subagent driving the koto session 'review-443'. `koto next review-443` returned:\n```json\n{\"action\":\"evidence_required\",\"state\":\"review\",\"directive\":\"Review the PR and record your verdict.\",\"expects\":{\"fields\":{\"verdict\":{\"type\":\"enum\",\"required\":true,\"values\":[\"approve\",\"reject\"]}}},\"blocking_conditions\":[],\"leg\":{\"request_id\":\"req-9f1c2b7a-4d3e-4c5f-8a1b-2c3d4e5f6071\",\"leg_name\":\"review\"},\"advanced\":true}\n```\nI've finished the review and I'm approving. Should I run `koto request resolve req-9f1c2b7a-4d3e-4c5f-8a1b-2c3d4e5f6071 review --with-data '{\"status\":\"success\",\"summary\":\"approved\"}'` to report my verdict on the leg, and then finish my workflow?",
      "expected_output": "Agent says no: the leg is bound to this session, so its result is promoted automatically from the session's terminal tick. The agent submits its evidence with `koto next review-443 --with-data '{\"verdict\": \"approve\"}'` and drives the workflow to terminal; koto writes the result onto the leg. An explicit resolve on a bound leg is rejected (`explicit_resolve_on_bound_leg`) and would block the real result.",
      "files": [],
      "assertions": [
        "Response tells the agent NOT to call `koto request resolve` on its own bound leg",
        "Response explains that a bound leg's result is promoted automatically when the child session reaches a terminal state",
        "Response mentions that koto rejects an explicit resolve on a bound leg (`explicit_resolve_on_bound_leg`), or that it would block the real result",
        "Response directs the agent to submit its verdict through `koto next review-443 --with-data` and complete the workflow normally"
      ]
    },
    {
      "id": 11,
      "name": "details-omitted-on-repeat-tick-without-arrival",
      "prompt": "I'm running a koto workflow called 'ship-it'. Ten minutes ago `koto next ship-it` returned:\n```json\n{\"action\":\"gate_blocked\",\"state\":\"preflight\",\"directive\":\"CI must pass before merge.\",\"details\":\"Run the full preflight checklist: lint, unit tests, and the smoke suite against staging. Fix any failures and re-run this command.\",\"advanced\":false,\"expects\":null,\"blocking_conditions\":[{\"name\":\"ci_check\",\"type\":\"command\",\"status\":\"failed\",\"category\":\"corrective\",\"agent_actionable\":true,\"output\":{\"exit_code\":1,\"error\":\"\"}}],\"error\":null}\n```\nI fixed nothing yet and just called `koto next ship-it` again to check. This time it returned:\n```json\n{\"action\":\"gate_blocked\",\"state\":\"preflight\",\"directive\":\"CI must pass before merge.\",\"advanced\":false,\"expects\":null,\"blocking_conditions\":[{\"name\":\"ci_check\",\"type\":\"command\",\"status\":\"failed\",\"category\":\"corrective\",\"agent_actionable\":true,\"output\":{\"exit_code\":1,\"error\":\"\"}}],\"error\":null}\n```\nWhy is `details` missing this time, and is this a bug I should report?",
      "expected_output": "Agent explains this is expected, not a bug: `details` is delivered when the workflow arrives at a state, and the second call did not move at all -- it re-evaluated the same failing gate and stayed on `preflight` -- so `details` is correctly omitted. It should NOT tell the user to report a bug or treat this as an error. It may mention `--full` to force `details` back, or `koto status ship-it` to retrieve it unconditionally.",
      "files": [],
      "assertions": [
        "Response states that the missing `details` on the second call is expected behavior, not a bug",
        "Response explains the delivery-on-arrival rule: `details` is delivered when the workflow arrives at a state and omitted on later ticks until it arrives again",
        "Response does NOT tell the user to report this as a bug or treat it as an error condition",
        "Response mentions at least one way to see `details` again on demand: `--full` on `koto next`, or `koto status ship-it`"
      ]
    },
    {
      "id": 12,
      "name": "details-redelivered-after-rewind",
      "prompt": "I'm running a koto workflow called 'audit-pass'. I already saw the `details` field once for the `remediate` state (it explained the remediation checklist step by step), then advanced past it. My reviewer just asked me to redo that step from scratch, so I ran `koto rewind audit-pass` and it landed back on `remediate`. If I call `koto next audit-pass` now, will I see the remediation checklist again, or do I need to pass `--full` to get it back?",
      "expected_output": "Agent explains that a `koto rewind` landing back on `remediate` counts as an arrival at that state, so the next plain `koto next audit-pass` call will deliver `details` again automatically -- `--full` is not required for this case (though it would also work).",
      "files": [],
      "assertions": [
        "Response states that `koto next audit-pass` will show `details` again without needing `--full`",
        "Response explains that a rewind into a state counts as an arrival, which is why `details` is delivered again",
        "Response does NOT claim the agent must pass `--full` to see `details` after the rewind",
        "Response distinguishes this from a plain repeat tick that does not move, where `details` would stay omitted"
      ]
    },
    {
      "id": 13,
      "name": "details-omitted-on-self-loop",
      "prompt": "I'm running a koto workflow called 'weekly-sweep'. Its `sync_week` state transitions to itself once per week of the horizon. On the first tick I got:\n```json\n{\"action\":\"evidence_required\",\"state\":\"sync_week\",\"directive\":\"Reconcile this week's calendar.\",\"details\":\"Full procedure: read the calendar for the target week, run the reconcile pass, and record each conflict with its resolution. If the calendar read fails, stop and report rather than guessing.\",\"advanced\":true,\"expects\":{\"fields\":{\"week_done\":{\"type\":\"enum\",\"required\":true,\"values\":[\"yes\",\"no\"]}}},\"error\":null}\n```\nI submitted `week_done: yes` and the workflow transitioned `sync_week -> sync_week` for week two. That response was:\n```json\n{\"action\":\"evidence_required\",\"state\":\"sync_week\",\"directive\":\"Reconcile this week's calendar.\",\"advanced\":true,\"expects\":{\"fields\":{\"week_done\":{\"type\":\"enum\",\"required\":true,\"values\":[\"yes\",\"no\"]}}},\"error\":null}\n```\nThe state is the same and `advanced` is true, so the workflow definitely moved -- why did `details` disappear?",
      "expected_output": "Agent explains this is expected: `details` is delivered on arrival at a state, and a self-transition is not an arrival -- the workflow went around a loop it was already in, so the agent is assumed to still hold the procedure. `advanced: true` means a transition fired, not that the agent arrived somewhere new. It should say the procedure will not come back on a later lap, and point at `koto status weekly-sweep` (or `koto next weekly-sweep --full`) as the way to get it back. It should NOT call this a bug, and it should NOT claim `details` will reappear on the next iteration.",
      "files": [],
      "assertions": [
        "Response states that the missing `details` after a self-transition is expected behavior, not a bug",
        "Response explains that a self-transition is not an arrival: the workflow looped inside the same state rather than entering it from somewhere else",
        "Response does NOT claim `details` will come back on a later lap of the loop",
        "Response names `koto status weekly-sweep` or `koto next weekly-sweep --full` as the way to retrieve the procedure"
      ]
    }
  ]
}
