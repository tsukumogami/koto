{
  "eval_id": 1,
  "configuration": "with_skill",
  "assertions": [
    {
      "text": "Response includes `koto init review-pr-443 --template .koto/code-review.md` (correct init command with session name and template path)",
      "passed": true,
      "evidence": "Line 20: `koto init review-pr-443 --template .koto/code-review.md` — exact match"
    },
    {
      "text": "Response includes `koto next review-pr-443` (correct loop command with the session name, not a generic placeholder)",
      "passed": true,
      "evidence": "Line 43: `koto next review-pr-443` — named correctly, not a placeholder"
    },
    {
      "text": "Response explains that the agent should inspect the `action` field in the JSON response and act accordingly (dispatch on action)",
      "passed": true,
      "evidence": "Line 46-47: 'This returns a JSON object. The `action` field tells you what to do next.' Plus full dispatch table follows."
    },
    {
      "text": "Response does NOT suggest `koto init` without `--template` or suggest a different command for starting a workflow",
      "passed": true,
      "evidence": "All init references include --template. No alternative init commands mentioned."
    }
  ],
  "pass_rate": 1.0
}
